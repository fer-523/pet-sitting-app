<style>
    .sidebar {
        width: 250px;
        height: 100vh;
        background-color: #343a40;
        color: #ffffff;
        position: fixed;
    }

    .sidebar-header {
        text-align: center;
        padding: 20px;
    }

    .user-avatar {
        width: 80px;
        border-radius: 50%;
        margin-bottom: 10px;
    }

    .sidebar-menu ul {
        list-style: none;
        padding: 0;
    }

    .sidebar-menu li {
        padding: 10px 20px;
    }

    .sidebar-menu li.active {
        background-color: #495057;
    }

    .sidebar-menu li a {
        text-decoration: none;
        color: #ffffff;
        display: flex;
        align-items: center;
    }

    .sidebar-menu li a i {
        margin-right: 10px;
    }

    .sidebar-menu li ul {
        padding-left: 20px;
    }

    .sidebar-menu li ul li {
        padding: 5px 0;
    }

</style>
<!-- Sidebar -->
<aside class="sidebar">
    <div class="sidebar-header">
        <img src="{{ asset('client/dashboard/assets/img/user-avatar.png') }}" alt="User Avatar" class="user-avatar">

    </div>

    <nav class="sidebar-menu">
        <ul>
            <!-- Home -->
            <li class="{{ app.request.get('_route') == 'client_home' ? 'active' : '' }}">
                <a href="#">
                    <i class="fas fa-home"></i> Home
                </a>
            </li>

            <!-- My Pets -->
            <li class="{{ app.request.get('_route') == 'app_pet_index' ? 'active' : '' }}">
                <a href="{{ path('app_pet_index') }}">
                    <i class="fas fa-paw"></i> My Pets
                </a>
            </li>

            <!-- Services -->
            <li>
                <a href="#servicesSubmenu" data-toggle="collapse" aria-expanded="false" class="dropdown-toggle">
                    <i class="fas fa-concierge-bell"></i> Services
                </a>
                <ul class="collapse list-unstyled" id="servicesSubmenu">
                    <li><a href="{{ path('app_pet_new') }}">Pet Sitting</a></li>
                    <li><a href="">Veterinary Services</a></li>
                    <li><a href="">Grooming</a></li>
                </ul>
            </li>

            <!-- Shop -->
            <li class="{{ app.request.get('_route') == 'app_pet_index' ? 'active' : '' }}">
                <a href="{{ path('app_pet_index') }}">
                    <i class="fas fa-shopping-cart"></i> Shop
                </a>
            </li>

            <!-- Appointments -->
            <li class="{{ app.request.get('_route') == 'app_pet_index' ? 'active' : '' }}">
                <a href="{{ path('app_pet_index') }}">
                    <i class="fas fa-calendar-alt"></i> Appointments
                </a>
            </li>

            <!-- Orders -->
            <li class="{{ app.request.get('_route') == 'app_pet_index' ? 'active' : '' }}">
                <a href="{{ path('app_pet_index') }}">
                    <i class="fas fa-box"></i> Orders
                </a>
            </li>

            <!-- Settings -->
            <li class="{{ app.request.get('_route') == 'app_pet_index' ? 'active' : '' }}">
                <a href="{{ path('app_pet_index') }}">
                    <i class="fas fa-cog"></i> Settings
                </a>
            </li>

            <!-- Logout -->
            <li>
                <a href="{{ path('app_logout') }}">
                    <i class="fas fa-sign-out-alt"></i> Logout
                </a>
            </li>
        </ul>
    </nav>
</aside>
