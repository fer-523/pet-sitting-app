{% extends 'base.html.twig' %}

{% block title %}Pet{% endblock %}

{% block body %}
    <h1>Pet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pet.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pet.name }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ pet.age }}</td>
            </tr>
            <tr>
                <th>Gender</th>
                <td>{{ pet.gender }}</td>
            </tr>
            <tr>
                <th>HealthInformation</th>
                <td>{{ pet.healthInformation }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ pet.photo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pet_index') }}">back to list</a>

    <a href="{{ path('app_pet_edit', {'id': pet.id}) }}">edit</a>

    {{ include('pet/_delete_form.html.twig') }}
{% endblock %}
